#include "./../TranquilityClient-SDK/client.h"
 
int main() {
    std::cout << "Hello World!\n";
    client_t* client = new client_t("conni@spookiebois.club", "123456789");

    //client->cache->guilds->size();

    std::cin >> reinterpret_cast<char*>((long)reinterpret_cast<char*>(new uint64_t));
}